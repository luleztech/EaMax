import 'package:flutter/material.dart';

/// Playback failed — close quietly (no technician / maintenance copy).
Future<void> showChannelUnavailableModal(BuildContext context) {
  return Future<void>.value();
}
